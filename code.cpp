#include <iostream>
#include <string>
#include <vector>
using namespace std;

struct info{
        string name;
        int roll_no;
        float cpi;
        string branch;  
};

int main(){

    struct info* newStudent = ( struct info* )malloc(sizeof(struct info));

    
    
    return 0;
}
